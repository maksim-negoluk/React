import React from 'react';

const Error = () => {
    return (
        <div>
            <h1 style={{color: 'red'}}>
            You have entered non existing page
            </h1>
        </div>
    );
};

export default Error;